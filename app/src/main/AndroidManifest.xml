<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cancilek.herdildeyazilim"
    android:versionCode="2"
    android:versionName="1.1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/hdy"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/hdy"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Hakkinda"></activity>
        <activity android:name=".CActivity" />
        <activity android:name=".JavaActivity" />
        <activity android:name=".CSharpActivity" />
        <activity android:name=".PythonActivity" />
        <activity android:name=".ContentActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>